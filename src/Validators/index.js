const email = require("./email.validator");
const mailbox = require("./mailbox.validator");
const template = require("./template.validator");

module.exports = {
    email,
    mailbox,
    template
}
